# v0.1.4 Compatibility Baseline

> **Archive notice:** Inherited unchanged from the Rust/Tauri repository. It has
> not been corrected or validated for Nestworth-go and is not current evidence.

> **Migration status:** Historical compatibility evidence copied from the previous implementation line. It does not describe the current Go + Fyne repository until independently reproduced.

This document freezes the implemented v0.1.3 surface that v0.1.4 must preserve. It is evidence for Phase 0, not a product-decision forum. Later phases add migration `004`, analytics services, analytics commands, and the `/analytics` route on top of this baseline without rewriting v0.1.1, v0.1.2, or v0.1.3 business rows.

Executable truth remains the current migrations, tests, generated bindings, and frozen command allowlist. This file records those values so later work can detect drift.

## Schema Freeze

- Current maximum embedded migration is `004` (`001_initial.sql`, `002_portfolio.sql`, `003_activity_history.sql`, `004_analytics.sql`) after Phase 2. Phase 0 froze the pre-004 surface at `003`.
- `max_supported_migration()` in `src-tauri/src/infrastructure/database_bootstrap.rs` derives its value from the embedded migrator, so it reports `4` once Phase 2 lands `004`.
- v0.1.4 added `004_analytics.sql` in Phase 2. Migration SQL must create schema only. It must not rewrite v0.1.1, v0.1.2, or v0.1.3 business rows, add triggers, or store financial facts as generic JSON.
- Migration `004` must not add columns to `activities` or `activity_legs`. Posted Activities are immutable evidence, and cost basis is a derived interpretation of that evidence rather than a new ledger fact.
- Unsupported future databases remain zero-write. Analytics reads must be blocked by the same state machine as Activity and history reads.

Sanitized fixtures:

- Released v0.1.1: `src-tauri/test-fixtures/v0.1.1.sql`
- Released v0.1.2: `src-tauri/test-fixtures/v0.1.2.sql`
- Activity and timezone goldens: `src-tauri/test-fixtures/v0.1.3-activity-goldens.md`

Phase 0 added `src-tauri/test-fixtures/v0.1.3.sql` and `src-tauri/test-fixtures/v0.1.4-analytics-goldens.md`. The v0.1.3 fixture contains posted Activities, a reversal, a correction chain, at least one closed-day snapshot revision, and at least one unknown-basis position.

## Frozen Command Allowlist

Copied exactly from `src-tauri/src/capabilities_test.rs` `ALLOWED_COMMANDS`. The list currently contains 81 commands after the per-holding gain batch command was added. Do not edit generated bindings by hand.

```text
bootstrap
complete_onboarding
list_members
create_member
update_member
archive_member
restore_member
list_institutions
create_institution
update_institution
archive_institution
restore_institution
list_groups
create_group
update_group
archive_group
restore_group
list_accounts
get_account
create_account
update_account
update_account_value
archive_account
restore_account
get_overview
set_member_avatar
set_institution_logo
set_group_logo
set_account_logo
set_instrument_logo
list_instruments
get_instrument
create_instrument
update_instrument
archive_instrument
restore_instrument
list_holdings
create_holding
update_holding
archive_holding
restore_holding
list_account_cash
append_account_cash
list_instrument_quotes
append_manual_instrument_quote
set_instrument_quote_preference
list_required_fx
list_fx_quotes
append_manual_fx_quote
set_fx_quote_preference
get_portfolio
search_provider_instruments
refresh_instrument
refresh_required_fx
refresh_all
get_media
get_settings
update_settings
delete_all_data
get_history_origin
confirm_history_timezone
preview_activity
list_activities
get_activity
create_activity
reverse_activity
correct_activity
get_account_timeline
get_history_status
rebuild_history_snapshots
get_net_worth_trend
get_analytics_status
get_performance_summary
get_gain_summary
list_holding_gain_summaries
get_net_worth_attribution
list_holding_lots
list_unknown_basis_lots
list_cost_basis_declarations
declare_lot_cost_basis
revoke_lot_cost_basis
```

The frozen production permission allowlist is unchanged and must stay unchanged:

```text
core:default
dialog:allow-open
log:allow-log
window-state:allow-filename
window-state:allow-restore-state
window-state:allow-save-window-state
```

## Frozen Generated IPC

`src/generated/tauri-bindings.ts` is the generated IPC surface. Command names match the 81-command allowlist above. Do not hand-edit the bindings.

## Frozen Ledger Facts Available to v0.1.4

This section records exactly what v0.1.3 persists, because v0.1.4 cost basis must be derived from these facts and nothing else.

### Activity header (`activities`)

| Column | v0.1.4 relevance |
| --- | --- |
| `kind` | Closed 15-value enum; identifies acquisitions, disposals, and remeasurement |
| `effective_at`, `effective_local_date` | Lot acquisition ordering and period boundaries |
| `created_at` | Deterministic tie-break at equal effective time |
| `reverses`, `corrects`, `correction_group` | Identifies economically nonexistent Activities |
| `income_kind` | Nine-value enum including `dividend` and `interest` |
| `fee_kind` | Eight-value enum including `brokerage_commission` |
| `related_instrument_id` | Attributes Income, Fee, Buy, and Sell to an Instrument |

### Activity leg (`activity_legs`)

| Column | v0.1.4 relevance |
| --- | --- |
| `role` | `source`, `destination`, `holding`, `settlement`, `fee`, `income`, `liability`, `adjustment` |
| `direction` | `increase` or `decrease`; carries the sign |
| `component_kind` | `account_value`, `holdings_cash`, `holding_quantity` |
| `amount`, `currency` | Gross settlement amount and fee amount for monetary legs |
| `holding_id`, `instrument_id`, `quantity` | Lot quantity for Quantity legs |
| `fx_rate` | Derived transaction rate on cross-currency cash Transfer legs |
| `sort_order` | Stable order within an Activity |

### Recorded gaps

These are facts about the implementation, not proposals. v0.1.4 design must respect them.

- **Trade unit price is not a persisted column.** `TradeSpec` accepts `unit_price` and `src-tauri/src/domain/activity.rs` validates `gross_amount == quantity × unit_price` through `expected_gross`, but only the resulting `gross_amount` is stored, on the settlement leg. Unit price is recoverable only as `gross ÷ quantity`, which is exact only up to the four-fractional-digit Money rounding applied when the gross amount is constructed. v0.1.4 must derive lot cost from the persisted gross amount and must not treat a recomputed unit price as an execution fact.
- **A zero-gross trade persists no settlement leg.** `trade()` appends the settlement leg only when `gross_amount` is non-zero, so a confirmed zero-unit-price Buy produces a Quantity leg and an optional Fee leg only. Its lot cost is zero plus any explicit fee.
- **Trade fees carry no `fee_kind`.** `trade()` sets `related_instrument_id` and leaves `income_kind` and `fee_kind` null, so a brokerage commission recorded inside a Buy or Sell is identified by `role = 'fee'` and its parent Activity, not by `fee_kind`. A v0.1.4 fee breakdown must not assume `fee_kind` is present on every fee leg.
- **Trade settlement currency equals the Instrument quote currency.** Enforced in `trade()`. v0.1.4 lot cost currency inherits that constraint.
- **Acquisition FX is not persisted.** No trade stores the native-to-base FX rate at execution time. v0.1.4 currency decomposition must select it historically with the existing at-or-before-cutoff quote rule and must report `unavailable` when no eligible quote exists.
- **Debt principal and interest are already separate.** Debt legs use `role = 'liability'`; interest and charges use an explicit Fee leg.

### Frozen classification derivation

`classify(kind, role)` in `src-tauri/src/domain/activity.rs` is the single source of wealth-flow classification, and `src-tauri/src/application/history_repositories.rs` mirrors it in SQL for filtering. The closed set is:

```text
external_inflow
external_outflow
internal_transfer
trade_principal
income
fee
debt_principal
remeasurement
```

v0.1.4 adds scope-relative flow classification for return calculation. It must not change, extend, or reinterpret this eight-value enum, and it must not let a caller supply a classification.

## Frozen Application-Layer Shape

The application layer contains **no service struct**. `rg "pub struct .*Service"` over `src-tauri/src` returns nothing. Every `pub struct` in `src-tauri/src/application/` is a DTO or an input type.

A "service" is a module of free `pub async fn`s. The documentation names map to modules as follows:

| Documentation name | Module |
| --- | --- |
| ValuationService | `application::valuation_service` |
| ActivityService | `application::activity_service` |
| HistoricalValuationService | `application::historical_valuation_service` |
| History snapshot rebuild | `application::history_snapshot_service` |
| History queries | `application::history_query_service` |

Public entry points take `state: &AppState`. Transaction-scoped variants take `&mut Transaction` and carry an `_in_tx` suffix. Repository SQL is centralized in `application::history_repositories`. Shared transaction helpers such as `begin_read_tx` and `begin_write_tx` live in `application::reference`.

v0.1.4 must follow this convention. Introducing a service struct would be a new architectural pattern and is out of scope.

Other frozen implementation facts:

- `Classification` is not a persisted column. It is derived on read by `classify(kind, role)` and mirrored in SQL only for list filtering.
- Trade legs never carry `fx_rate`, and the `activity_legs` `CHECK` constraint forbids `fx_rate` on `holding_quantity` legs entirely. FX rates appear only on Transfer and Debt legs.
- `MAX_REBUILD_DAYS` is `366` in `application::history_snapshot_service`.
- `ErrorCode` in `src-tauri/src/error.rs` has 45 variants. `FORBIDDEN_TRACING_FIELDS` in `capabilities_test.rs` has 32 entries and includes `amount`, `quantity`, `unit_price`, `legs`, `raw_legs`, `quote`, `symbol`, `cost`, `basis`, `gain`, `proceeds`, `return`, and `rate`.

## Frozen Frontend Facts

These are implementation facts that v0.1.4 frontend phases must work with rather than rediscover.

| Fact | v0.1.4 consequence |
| --- | --- |
| No chart library is installed; `TrendChart` is hand-rolled SVG in `src/features/overview/net-worth-trend.tsx` with geometry helpers in `net-worth-trend-model.ts` | Analytics charts follow the same pattern; adding a chart dependency is out of scope |
| `src/test/setup.ts` mocks all 80 generated commands as `vi.fn()` | Every new analytics command must be added there or unrelated page tests fail on an undefined command |
| `src/features/overview/overview-page.test.tsx` asserts that `return`, `gain`, `benchmark`, and `attribution` are absent from the Overview document | Phase 9's Overview attribution link deliberately changes these four assertions; they are a scope guard, not a defect |
| The `NavLink` `to` prop in `src/components/app-shell.tsx` is a hardcoded union of nine literal paths | Adding `/analytics` requires extending that union |
| Locale files are `src/locales/{en,zh-CN}/common.json`, one `common` namespace, 18 top-level keys and 674 leaf keys each, parity enforced by `src/lib/i18n/locales.test.ts` | Analytics adds one top-level namespace with identical key sets in both files |
| `invalidateValuation` in `src/lib/tauri/invalidate.ts` is a coarse 12-prefix invalidation used by valuation-affecting writes | A cost-basis declaration must not use it, because no valuation query can change; it invalidates only gain and lot queries |
| `useInfiniteQuery` is not used anywhere; cursor pagination is manual and URL-driven, as in `src/features/activity/activity-page.tsx` | Lot and declaration lists follow the same manual cursor pattern |
| `components/ui` contains only `button`, `input`, and `textarea`; there is no Card, Table, Dialog, or Badge primitive, and `cn()` performs no class-conflict merging | Analytics panels compose inline with Tailwind classes like every existing page |
| Vitest runs with `fileParallelism: false` because the app shares module-level `queryClient` and `router` singletons | New tests must not assume isolation from that shared state |

## Frozen Goldens

### v0.1.1 released fixture after migrate to `003`

| Field | Value |
| --- | --- |
| Assets | `125000` CNY |
| Liabilities | `5000` CNY |
| Net worth | `120000` CNY |
| `account_count` | `3` |

### Golden holdings portfolio (`62190` / `63190` CNY)

| Component | Native facts | Base CNY |
| --- | --- | --- |
| QQQ | `3` × `700` USD | `3 × 700 × 6.9 = 14490` |
| ES3 | `1000` × `4` SGD | `1000 × 4 × 5.3 = 21200` |
| Holdings cash | `5000` SGD | `5000 × 5.3 = 26500` |
| Holdings Account total | | `62190` |
| Separate Manual Value Account | `1000` CNY | `1000` |
| Household assets / net worth | | `63190` |

Locked market inputs remain FX `1 USD = 6.9 CNY`, FX `1 SGD = 5.3 CNY`, and Household base currency `CNY`.

### v0.1.3 Activity goldens

From `src-tauri/test-fixtures/v0.1.3-activity-goldens.md`. These remain unchanged by v0.1.4:

| Scenario | Frozen result |
| --- | --- |
| Internal cash transfer | `10000 → 7000 + 3000`; net worth `10000`; external flow `0` |
| Buy with fee | `2 QQQ × 100 USD` plus `5 USD` fee leaves `795 USD` cash, Quantity `2`, principal `200`, fee `5` |
| Cross-currency transfer | `100 USD → 690 CNY` derives `FxRate 6.9`, classification internal |
| Cross-currency spread | `1000 CNY → 186.9 SGD` at market `1 SGD = 5.3 CNY` yields destination base `990.57 CNY` and spread `9.43 CNY` loss |
| History Origin baseline | `0` posted Activities after migrating the v0.1.2 fixture |

Every v0.1.4 analytics result must leave these values byte-identical. Adding a cost-basis declaration must not change any of them.

## Frozen Decimal Contract

| Type | Integer digits | Fractional digits | Sign |
| --- | --- | --- | --- |
| Money | 1–12 | 0–4 | Unsigned; maximum `999999999999.9999` |
| Quantity | Up to 18 | Up to 8 | Unsigned; zero allowed |
| UnitPrice | Up to 12 | Up to 8 | Unsigned; zero allowed |
| FxRate | Up to 8 | Up to 12 | Unsigned; greater than zero |

`rust_decimal` is declared as `rust_decimal = "1"` in `src-tauri/Cargo.toml` with no extra features. Valuation retains full checked `Decimal` precision and rounds only at Money boundaries using four fractional digits and midpoint-nearest-even.

Every existing financial value is unsigned. v0.1.4 introduces the first signed financial results in the product, so it must add new types rather than relaxing `Money`. Enabling the `rust_decimal` `maths` feature for `powd`, `ln`, and `exp` is a Phase 1 engineering deliverable; `f32`, `f64`, SQLite `REAL`, and JavaScript `number` remain prohibited for every financial value including return rates.

## Frozen Product Decisions

These are already approved in the [release contract](v0.1.4.md). They are frozen here so Phase 1 and later do not reopen them:

- Lot policy is FIFO only. There is no average-cost mode, no user lot selection, and no per-Instrument policy switch.
- Cost basis is a derived interpretation of the immutable Activity ledger. Analytics never post an Activity and never change current state, net worth, Quantity, or a snapshot.
- Origin and adjustment quantities remain unknown-basis until the user explicitly declares a basis. Nestworth never infers one from the first historical quote.
- Fees are never capitalized into cost basis. Gross and net gain are reported separately.
- No benchmark, index series, or peer comparison ships in v0.1.4.
- No live quote or FX vendor is selected or required. All analytics remain manually operable offline.
- Snapshots stay closed-day, append-only revisions produced only by explicit rebuild.

## Expected Query Counts

The v0.1.3 counts below are carried forward unchanged and must not regress.

### Overview (`get_overview`)

| Item | Count |
| --- | --- |
| Unique statement families | 12 |
| SQL executions in the current call graph | 19 |
| N+1 per Account | none |

### Portfolio (`get_portfolio`)

| Item | Count |
| --- | --- |
| Unique statement families | 9 |
| SQL executions in the current call graph | 18 |
| N+1 per Account | none |

### Account detail (`get_account`)

| Item | Count |
| --- | --- |
| Unique statement families | 9 |
| SQL executions in the current call graph | 11 |
| N+1 per Holding or cash row | none |

### Analytics reads

`N/A until Phase 7`. Planned query families, each Household-scoped and bounded rather than per-lot or per-day:

- History Origin header and baseline holdings
- Ordered Activity headers and legs for the analyzed Instruments and Accounts, loaded in one batch
- Cost-basis declarations for the analyzed lot identities, loaded in one batch
- Daily snapshot headers for the analyzed local date range
- Daily snapshot items for those headers, loaded in one batch
- Latest Instrument and FX Quotes for current unrealized value
- Historical FX Quotes at the acquisition times required by currency decomposition
- Snapshot dirty-range state

Phase 7 must keep these bounded. A query inside a loop over lots, Activities, snapshot days, or snapshot items remains prohibited. The FIFO replay and every return calculation run in memory over one consistent read snapshot.

## Compatibility Tests

- `released_v011_fixture_migrates_without_changing_overview_or_relationships` must keep passing at migration version `4`.
- `released_v012_fixture_loads_on_schema_002_without_changing_golden_totals` and its schema-3 companions must keep the `62190` / `63190` goldens.
- A new v0.1.3 fixture test must migrate to version `4`, keep every Activity, leg, snapshot revision, and correction link byte-identical, and confirm that migration `004` creates zero cost-basis declarations.
- Unsupported future migration version `5` must receive zero application writes from bootstrap, Activity and history commands, and every analytics command.
