# v0.3.5 Release Contract

## Status

`v0.3.5` / build `6` is the unreleased development line, opened on 2026-09-20.
The release owner has authorized the version increment only; feature scope
has not been assigned. Existing capabilities and schema `11` carry forward
from [v0.3.4](v0.3.4.md). No database migration is introduced by this change.

## Metadata

Go runtime, frontend package, Wails configuration, macOS plists and build
flags, Windows manifests/installer metadata, and Linux packaging identify
version `0.3.5`. Runtime and macOS build number is `6`.
Bundle identity remains `com.nestworth.app`.

Expected macOS outputs are `dist/macos/Nestworth.app`,
`Nestworth-0.3.5-arm64.dmg`, and `Nestworth-0.3.5-arm64.zip`.

## Validation boundary

This increment verifies version metadata and the existing version test only.
Full application checks, production packaging, native acceptance, provider
requests, signing, and notarization have not been rerun for `0.3.5`.
The v0.3.4 release evidence remains historical and is not a v0.3.5 result.

Before publication, run the [local validation and packaging workflow](../development/local-workflow.md),
record current results here, and review the upgrade and native acceptance
checklist carried forward from v0.3.4.
